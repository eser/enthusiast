import fromNodeStream, { CustomIterator } from './fromNodeStream';
import toNodeStream from './toNodeStream';

const library = {
    fromNodeStream,
    CustomIterator,
    toNodeStream,
};

export {
    library as default,
    fromNodeStream,
    CustomIterator,
    toNodeStream,
};
